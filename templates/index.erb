<h2>Superwalllll</h2>

<div id="post-form">
  <form action="/posts" method="post">
    <div>
      <textarea name="body"></textarea>
    </div>
    <div>
      <input type="submit" value="Post">
    </div>
  </form>
</div>
<ol class="post-list">
  <% posts.each do |post| %>
    <li>
      <p><%= post.created_at.strftime('%A, %B %-d, %Y') %></p>
      <p>
        <form action="/posts/<%= post.id%>/upvote" method="post">
          <p><strong>Upwalls</strong>:<%= post.votes %></p>
          <input type="submit" value="Upwall!">
        </form>
      </p>  
      <p>
        <form action="/posts/<%= post.id%>/downvote" method="post">
          <p><strong>Downwalls</strong>:<%= post.votes %></p>
          <input type="submit" value="Downwall!">
        </form>
      </p>  
      <p><%= escape(post.body) %></p>
      <form action="/posts/<%= post.id %>/delete" method="post">
        <div class="delete">
          <input type="submit" value="Delete">
        </div>
      </form>  
    </li>
  <% end %>
</ol>